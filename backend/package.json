{
  "name": "heartfelt-backend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'No build needed for Cloudflare Workers'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.4",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "wrangler": "^4.63.0"
  }
}
